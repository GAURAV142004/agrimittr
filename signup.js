function signup(event) {
    event.preventDefault(); // Prevent form submission

    // Perform signup logic here (e.g., validate inputs, store user data)

    // Redirect to the home page after signup
    window.location.href = "index.html";
}
